server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/antievil?serverTimezone=UTC
    username: root
    password: well1995
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true